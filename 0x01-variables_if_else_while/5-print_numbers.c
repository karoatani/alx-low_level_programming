#include <stdio.h>
/**
 * main - Entry point of the function
 * Description - Prints 0-9
 * return - Always return 0
 */

int main (void)
{
    int i;
    for(i=0; i < 10; i++)
        printf("%d",i);
    printf("\n");
    return (0);
}
